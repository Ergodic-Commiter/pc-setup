The script `.config/scripts/toggle-colors.sh` changes between Light and Dark mode.  
It would be great to set it up with a button in the Brightness applet.  
In the meantime the order is the following: 
1. Activate it via the command line. 
2. Create an application that runs the command.  
3. Create an independent button in the System Tray.  
4. Include the button inside the Brightness applet.  